# -*- coding: utf-8 -*-
"""
Реализуйте класс Die, отражающий игральную кость с произвольным числом граней.

Класс должен отвечать следующим требованиям:

-   метод __init__ класса Die должен принимать параметр faces, отражающий число
    граней кости, со значением по умолчанию, равным 6. 

-   экземпляры класса Die должны иметь метод roll(), отражающий бросок игральной
    кости. Метод roll() должен возвращать случайное число в интервале от 1 до N
    (включая границы), где N - число граней кости.

Создайте экземпляры Die с 6, 16 и 20 гранями.
"""


from random import randint


class Die:
    ### ваше решение: ###
from random import randint
 
class Die:
    def __init__(self, faces=6):
        self.faces = faces
 
    def roll(self):
        return randint(1, self.faces)
 
# Создание экземпляров Die с разным количеством граней
die_6 = Die()  # Кость с 6 гранями (по умолчанию)
die_16 = Die(faces=16)  # Кость с 16 гранями
die_20 = Die(faces=20)  # Кость с 20 гранями
 
# Проверка работы броска кости
print(die_6.roll())  # Бросок кости с 6 гранями
print(die_16.roll())  # Бросок кости с 16 гранями
print(die_20.roll())  # Бросок кости с 20 гранями
